# Emoji

The `Emoji` component disables WordPress emoji scripts, styles, and filters.

```php
use Toybox\Core\Components\Emoji;
```

## Methods

### `disable()`

Disables WordPress's built-in emoji library from being included on pages.

```php
public static function disable(): void
```

**Returns:** void

