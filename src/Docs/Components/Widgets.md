# Widgets

The `Widgets` component removes the default WordPress widgets.

```php
use Toybox\Core\Components\Widgets;
```

## Methods

### `unregisterDefaults()`

Unregisters default widgets if they aren't being used.

```php
public static function unregisterDefaults(): void
```

**Returns:** void

