# Visitor

The `Visitor` component detects a visitor's first request using a cookie.

```php
use Toybox\Core\Components\Visitor;
```

## Methods

### `firstVisit()`

If it is the visitor's first visit to the website.

```php
public static function firstVisit(): bool
```

**Returns:** bool

